DATABASE_URL=file:local.db

# DeepL API (번역용). 서버에만 설정. 없으면 번역 버튼 비활성/안내만 표시.
# DEEPL_AUTH_KEY=

# SUNO 구독 실데이터 연동 (선택)
# - 값이 있으면 /api/suno/subscription GET에서 원격 데이터를 우선 조회합니다.
# - 실패 시 내부 DB/기본값으로 자동 폴백합니다.
# SUNO_SUBSCRIPTION_SOURCE_URL=
# SUNO_SUBSCRIPTION_SOURCE_API_KEY=
# SUNO_SUBSCRIPTION_SOURCE_BEARER_TOKEN=
